<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:installLocation="auto">

    <!-- ==== Storage (the required .txt export to secondary volumes) ==== -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <!-- All-Files access on Android 11+ (best effort; used only if the user
         grants it or a permissive/rooted ROM already reports it). -->
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <!-- ==== Read-only info gathering (normal permissions, auto-granted) ==== -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- INTERNET is used ONLY by the optional, user-triggered speed test. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />

    <!-- ==== Extended device info (runtime-requested "dangerous" perms) ==== -->
    <!-- Telephony: operator, SIM, network type, cell info, signal. IMEI/IMSI/
         serial are OS-blocked for normal apps on Android 10+ (shown as such). -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_NUMBERS" />
    <!-- Location: last-known GPS, cell info, and Wi-Fi SSID/scan (targetSdk 29
         uses the location model for Wi-Fi, not NEARBY_WIFI_DEVICES). -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Bluetooth: paired devices and adapter name on Android 12+. -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- ==== Compatibility: install on TV, phones, tablets, boxes alike ==== -->
    <!-- Nothing is truly required, so the widest set of devices can install. -->
    <uses-feature android:name="android.software.leanback" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.hardware.faketouch" android:required="false" />
    <uses-feature android:name="android.hardware.telephony" android:required="false" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-feature android:name="android.hardware.wifi" android:required="false" />
    <uses-feature android:name="android.hardware.location" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:banner="@drawable/banner"
        android:label="@string/app_name"
        android:hardwareAccelerated="true"
        android:requestLegacyExternalStorage="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|uiMode|navigation|density">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <!-- Phones / tablets home screen -->
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- Android TV home screen -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Serves the generated .txt as a content:// URI so it can be shared
             to any app (email, Drive, WhatsApp, ...). Custom provider = no
             AndroidX dependency. -->
        <provider
            android:name=".ShareTxtProvider"
            android:authorities="com.universal.deviceinfo.files"
            android:exported="false"
            android:grantUriPermissions="true" />
    </application>
</manifest>
